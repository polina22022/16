
# Работа с массивами в C
## 📋 Задания к лаб. 11
1.Заполнение массива с консоли

2.Преобразование массива

3.Печать массива

4.Вычисление количества положительных элементов массива, расположенных до
максимального элемента.

## 📋 Задания к лаб. 14
1.Заполнение массива значениями из функции f(x)

2.Печать массива

3.Вычисление суммы элементов массива

4.Поиск максимального значения, среди элементов порядковый номер которых лежит в
диапазоне [K; L]

## 📋 Задания к лаб. 16



## 📁 Структура проекта

lab218/

├── src/

│ ├── arrays.h

│ ├── arrays.c

│ └── main.c

├── tests/

│ ├── test_arrays.c

│ └── run_tests.sh

├── Makefile

└── README.md

## 🔧 Реализованные функции


### 1.double* full_elements(double* arr, int size)
- Назначение: Заполнение массива вещественными числами, вводимых с клавиатуры
- Параметры:
  - arr - указатель на массив
  - n - размер массива
- Возвращает: указатель на заполненный массив

### 2.double put_elements(double* arr, int size);
- Назначение: Печать элементов массива
- Параметры:
  - arr - указатель на массив 
  - size - размер массива
- Возвращает: количество выведенных элементов

### 3.double calc_elements(double* arr, int size);
- Назначение:  Преобразование элементов массива (изменение знака)
- Параметры:
  - ptr_array - указатель на массив
  - size - размер массива
- Возвращает: указатель на преобразованный массив

### 4.double find_max_in_range(double* arr, int size, int K, int L);
- Назначение: Поиск максимального значения в массиве в заданном диапазоне индексов [K; L]
- Параметры:
   - arr: указатель на массив для поиска
   - size: общий размер массива
   - K: начальный индекс диапазона (включительно)
   - L: конечный индекс диапазона (включительно)
 - Возвращает: максимальное значение 


